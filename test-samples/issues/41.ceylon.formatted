void testIssue41() {
    value s = "String template with:
               * here: ``1`` no spaces;
               * here: ``1.string`` no spaces either;
               * here: `` 3 - 1 `` spaces;
               * here: ``(3 - 1)`` no spaces because of parens;
               * here: ``(3 - 1).or(5)`` no spaces again.";
}
